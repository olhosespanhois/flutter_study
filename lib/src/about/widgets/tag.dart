import 'package:flutter/material.dart';

class TagLdj extends StatelessWidget {
  const TagLdj({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'images/tag.png',
      alignment: Alignment.center,
      width: 200,
    );
  }
}
